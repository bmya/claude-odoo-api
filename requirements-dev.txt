# Development and test dependencies.
#   pip install -r requirements.txt -r requirements-dev.txt

pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Needed by the HTTP-layer tests. starlette arrives transitively via mcp and
# httpx via the Starlette TestClient, but the tests import both directly, so they
# are declared here rather than relied upon by accident.
starlette>=0.37.0
httpx>=0.27.0

# Formatting / linting
black>=24.0.0
isort>=5.13.0
flake8>=7.0.0
